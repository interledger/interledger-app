{{- define "interledger-app.frontend.deployment" -}}
{{- $top := first . }}
{{- if $top.Values.frontend.enabled }}
{{- $cfgObj := include "interledger-app.frontend.configMap" $top | fromYaml -}}
{{- $dataHash := $cfgObj.data | toJson | sha256sum -}}
spec:
  template:
    metadata:
      annotations:
        ilf-configmap-frontend-sha256: {{ $dataHash | quote }}
    spec:
      volumes:
        - name: config
          configMap:
            name: {{ include "common.fullname" $top }}-frontend-config
{{- end }}
{{- end }}
{{- if .Values.frontend.enabled }}
{{- include "common.deployment" (list . .Values.frontend.server .Values.frontend.server.autoscaling .Values.frontend.server.serviceAccount "interledger-app.frontend.deployment") }}
{{- end }}
