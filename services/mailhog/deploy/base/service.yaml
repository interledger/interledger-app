apiVersion: v1
kind: Service
metadata:
  name: mailhog
  labels:
    app: mailhog
spec:
  ports:
    - port: 1025
      targetPort: 1025
      name: smtp
    - port: 80
      targetPort: 8025
      name: http
  selector:
    app: mailhog
