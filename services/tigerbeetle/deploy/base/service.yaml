apiVersion: v1
kind: Service
metadata:
  name: tigerbeetle
  labels:
    app: tigerbeetle
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  clusterIP: None
  selector:
    app: tigerbeetle