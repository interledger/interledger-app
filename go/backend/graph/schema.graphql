# GraphQL schema for Fynbos control plane

type Organisation {
  id: ID!
  name: String!
}

type Query {
  organisation (id: String!): Organisation
}

type Mutation {
  createOrganisation (name: String!): Organisation
}
