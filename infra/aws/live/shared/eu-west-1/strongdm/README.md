This deploys a StrongDM gateway into the shared infrastructure. This allows StrongDM grant access to items
in the shared infra.

### Prerequisites

In order to deploy this stack, make sure to get an `SDM_TOKEN` from the Strongdm website and put it in your env.