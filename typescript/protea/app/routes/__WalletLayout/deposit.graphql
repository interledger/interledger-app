query GetFundingSources {
  fundingSources {
    id
    name
    verificationStatus
    mask
    type
    subType
  }
}

input DepositInput {
  fundingSourceID: ID!
  amount: String!
}

mutation InitiateDeposit($input: DepositInput!) {
  initiateDeposit(input: $input) {
    code
    success
    message
    transaction {
      id
      type
      description
      amount
      timestamp
      status
    }
  }
}
