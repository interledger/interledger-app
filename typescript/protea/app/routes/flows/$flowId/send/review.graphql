mutation InitiateOutgoingPayment($input: OutgoingPaymentInput!) {
  initiateOutgoingPayment(input: $input) {
    code
    success
    message
    outgoingPayment {
      id
      destination
      state
      amount
      timestamp
    }
  }
}
