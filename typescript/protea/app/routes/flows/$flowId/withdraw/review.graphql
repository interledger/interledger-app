mutation InitiateWithdrawal($input: WithdrawalInput!) {
  initiateWithdrawal(input: $input) {
    code
    success
    message
    withdrawal {
      id
      timestamp
      amount
      state
    }
  }
}
