/**
 * TODO Need to add event listener for keyboard events
 * This should not interfere with localised keyboard shortcuts at the component level
 *
 * window.document.addEventListener(this.props.keyEventName, this.handleKey)
 *
 *
 * Custom keyboard shortcuts
 * w - route to /withdraw ? We don't currently have a route for this as we specify an account to withdraw from
 * ctrl/cmd + k - open command palette
 */
