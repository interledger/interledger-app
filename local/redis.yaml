services:
  # 1. Redis server
  redis:
    image: valkey/valkey:8-alpine
    profiles:
      - infrastructure
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data: